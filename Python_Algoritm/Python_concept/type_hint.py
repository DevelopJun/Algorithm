'''
여기서 타입힌트
파이썬은 동적 타이핑 언어임에도 타입을 지정할 수 있는타입 힌트가 추가 되었다, 
'''

a: str = '1'
b: int = 1

# 이런식으로 사용하면 된다. 함수에서 넘어오는 파라미터가 무엇인지


def fn(a: int) -> bool:
    pass

# 이런식으로 명시해주면 리턴 값으로 boolean 값을 가질 수 있다는 것을 알게된다.
